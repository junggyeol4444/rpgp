/**
 * 지역별 고정 레벨대. 몬스터를 플레이어 레벨에 맞춰 스케일링하지 않는다.
 *
 * <p>지시서 3장 [RegionService / RegionDefinition] 의 자리다.
 *
 * <p>구현 단계: 2단계 이후 필요 시점.
 * 지시서 14장에 따라 해당 단계에 들어가기 전에는 여기에 코드를 두지 않는다.
 */
package com.example.rpgcore.region;
